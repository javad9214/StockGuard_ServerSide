spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/stockguard_dev
    username: devuser
    password: devpass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080
