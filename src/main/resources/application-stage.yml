spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/stockguard_stage
    username: stageuser
    password: stagepass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8081
