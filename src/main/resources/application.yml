# application.yml
spring:
  datasource:
    url: jdbc:postgresql://185.4.16.24:22016/postgres
    username: root
    password: ngxOnzCyA641mQKsHCrUJQ==

  logging:
    level:
      root: INFO
      your.package.name: DEBUG

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: ${JWT_SECRET}
  expiration: 900000          # 15 minutes
  refresh:
      expiration: 2592000000    # 30 days

server:
  port: 8080
